<?php

/**
 * Implements hook_theme().
 */
function auth_block_theme($existing, $type, $theme, $path) {
    return array(
        'auth_block__sidebar' => array(
                 'variables' => array('title' => NULL, 'description' => NULL),
             ),
       );
}
